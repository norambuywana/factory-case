# Factory case

## Installation

```bash
npm install
```

## Run locally

Build and run docker containers

```bash
docker compose up --build
```
Build and run docker containers

```bash
docker compose up --build
```